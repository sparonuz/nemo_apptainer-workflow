JOBS:
  LOCAL_SETUP:
    FILE: script/local_setup.sh
    PLATFORM: LOCAL
    RUNNING: once

  REMOTE_SETUP:
    FILE: script/remote_setup.sh
    PLATFORM: MN5_LOGIN
    DEPENDENCIES: LOCAL_SETUP
    WALLCLOCK: 00:05
    RUNNING: once
